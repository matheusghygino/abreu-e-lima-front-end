name: Deploy VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Executar deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 173.249.13.43
          username: abreuelima
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/abreuelima/public_html
            git pull origin main
            npm install
            npm run build